declare module Types {
  export interface Trip {
    id: string;
    destination: string;
    title: string;
    description: string;
    cost: number;
    createdAt: string;
    updatedAt: string;
  }
}
